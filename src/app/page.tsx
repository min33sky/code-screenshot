import AppClient from '@/components/app-client';

export default function Home() {
  return (
    <main className="min-h-screen flex justify-center bg-slate-900 text-white items-center">
      <AppClient />
    </main>
  );
}
